
let obj = {
    
    name: "Oladele",
    age: 100,
    gender: "Male"
};
    if(obj.age >= 18 && obj.age <= 75){
        console.log(`Allow your age is ${obj.age}`);
    }
    else    {
        console.log(`Disallow your age is ${obj.age}`);
    }
    
  // Write a program that will detect if the number is even number or odd 

// That will detect if 